{
  "name": "chathub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python -m http.server 8000",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  }
}
